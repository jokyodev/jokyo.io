import { Button } from "@/components/ui/button";

const CourseEnrollChecker = () => {
  return (
    <div>
      <Button className="w-full">Thanh toán khóa học</Button>
    </div>
  );
};

export default CourseEnrollChecker;
